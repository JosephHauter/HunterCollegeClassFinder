{
  "name": "huntercollegeclassfinder",
  "version": "1.0.0",
  "description": "Finds which classes are avaliable for whatever major, for now building for Computer science classes will change later on to support all classes",
  "main": "discordWebhook.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  }
}
